@model TicketPeru.Models.ForgotPasswordViewModel



<!-- ACCOUNT SETTINGS SECTION -->
@*<section class="settingSection">
    <div class="container">
        <div class="row">
            <div class="col-md-3 hidden-xs hidden-sm"></div>
            <div class="col-md-6 col-xs-12 col-sm-12">
                <div class="settingContent bg-ash text-center" style="padding: 20px;">
                    <h4><i class="fa fa-address-card-o" style="color:#262a2e;" aria-hidden="true"></i>&nbsp;¿Olvidaste tu contraseña?</h4>
                    <div class="row text-center">
                        @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary("", new { @class = "text-danger" })<br />
                            <div class="col-sm-12">
                                @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @placeholder = "Email", @type="email" })
                            </div>
                            <div class="col-sm-12">
                                <button type="submit" class="btn btn-block btn-primary" style="width: 100%;">Enviar Link</button>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <div class="col-md-3 hidden-xs hidden-sm"></div>
        </div>
    </div>
</section>*@




    <!-- Start Forgot-Password -->
<section class="forgot-Password section">
    <div class="container">
      @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-modal bg-white br", role = "form" }))
      {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary("", new { @class = "text-danger" })<br />
            <legend class="text-center">
                <span class="icon-user yellow"></span>
                <h2 class="h2 blue">¿Olvidaste tu contraseña?</h2>
            </legend>
            @Html.TextBoxFor(m => m.Email, new { @class = "br", @placeholder = "Email", @type = "email" })
            <button class="btn btn-blue br" type="submit" name="button">Enviar link</button>
      }
    </div>
</section>
<!-- End Forgot-Password -->

