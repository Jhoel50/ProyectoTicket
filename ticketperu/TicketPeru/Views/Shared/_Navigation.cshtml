<nav class="navbar navbar-default navbar-main   lightHeader " role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" style="margin-left: 10px !important;" href="@Url.Action("Index","Home")"></a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <div class="col-md-4 col-xs-12 pull-right navbar-search-custom">
                @using (Html.BeginForm("Index", "Home"))
                {
                    <div class="input-group">
                        <input id="Buscar" name="Buscar" class="form-control" placeholder="Buscar" />
                        <span class="input-group-addon" id="spanBuscar" onclick="buscar()" style="cursor: pointer; background-color: #e30405; border: 0; color: #fff; border-top-right-radius: 4px; border-bottom-right-radius: 4px;"><i class="fa fa-search" aria-hidden="true"></i></span>
                        <button type="submit" id="btnBuscar" hidden></button>
                    </div>
                }
            </div>
        </div>
    </div>
</nav>